import React, { Component } from 'react';
import { CardContent } from '@material-ui/core';

class Footer extends Component {
  render() {
    return (
      <CardContent>Footer with connection information</CardContent>
    )
  }
}

export default Footer;
